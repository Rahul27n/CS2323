Instructions to execute the source code in Lab8.py :

1. Include the data.txt and Lab8.py files in your working directory. Modify the data file accordingly if needed. The Python program can also be modified to print disassembled code with
   (or) without textual labels by commenting (or) uncommenting the last 2 lines accordingly.

2. On a Linux system, go to the terminal and execute the command -- python3 Lab8.py < data.txt. Ensure that python3 is installed on the system.

3. The output will be displayed on the terminal. To test for other inputs, modify the data file again and repeat above steps.